import os

# Placeholder: real logic comes later
def remove_watermark(video_path: str) -> str:
    print(f"🧼 [watermark_remover] Removing watermark from {video_path} (placeholder)")
    return video_path
